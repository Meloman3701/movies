import React, { memo } from 'react';
import styles from 'styles/MoviesDetail.module.scss';

const MovieDetail = () => {
  return (
    <div className={styles.page}>work</div>
  )
}

export default memo(MovieDetail);
